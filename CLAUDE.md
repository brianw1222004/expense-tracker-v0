# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Expense-tracker demo built with Expo SDK 56 / React Native 0.85 (React 19). Plain JavaScript, no TypeScript. No linter configured. `npx expo export --platform web` is the build check (catches syntax/import errors). `dist/` is the output of that export — generated, not source.

Backend is Supabase (auth + Postgres), configured via `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY` in `.env` (gitignored; template in `.env.example`, schema in `supabase/schema.sql`, one-time setup steps in `SUPABASE_SETUP.md`). **With no `.env` the app runs in local-only mode** — no sign-in screen, AsyncStorage only — so it always boots even without a backend; don't write code that assumes `supabase` is non-null (it's null when unconfigured).

## Commands

```
npm start                  # Expo dev server (LAN — phone must be on same Wi-Fi)
npx expo start --tunnel    # dev server through ngrok tunnel (works across networks)
npm run web                # run in the browser via react-native-web
npm run ios                # build & run the native iOS dev client (expo run:ios; an ios/ prebuild exists)
npm run android            # build & run the native Android dev client (expo run:android; generates android/ on first run)
npm test                   # run Jest test suite (11 suites in src/__tests__/, incl. splits-regression, sync-groups-splits, storage, budget)
npm test -- --testPathPatterns=categories  # run a single test file (note: plural --testPathPatterns)
npx expo export --platform web      # build check (catches syntax/import errors); regenerates dist/
node scripts/make-qr.js [exp-url]   # write a scannable QR PNG for a tunnel URL to ../expense-tracker-qr.png
```

Tests live in `src/__tests__/` with mocks in `__mocks__/` (react, react-native, async-storage, supabase-js, i18n). Jest config is in `jest.config.js`; tests run in a Node environment with babel-jest transform. When adding a new test, follow the existing pattern: use Jest's auto-injected globals (`describe`, `test`, `expect`) without importing them — require the module under test directly, and use the module-level mocks.

The app is run on a phone via Expo Go (scan the QR from the terminal), or in a browser with `npm run web`. Code must work on BOTH targets — e.g. `Alert.alert` with buttons is a no-op on web (use a custom Modal or `window.confirm` fallback), and `onEndEditing` never fires on web (use `onBlur`).

## RN 0.85 gotchas (verified against the v56 docs)

- **Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.** Expo APIs change between SDK versions; don't rely on memory or unversioned examples.

- `StyleSheet.absoluteFillObject` was removed; use `StyleSheet.absoluteFill` as a style entry.
- Classic `Animated` is the supported animation API here (no reanimated). `useNativeDriver: true` only for `transform`/`opacity`; animating SVG props (`strokeDashoffset` on `Animated.createAnimatedComponent(Path)`) requires `useNativeDriver: false`. Never mix drivers on one `Animated.Value`.

## Theme, font, language — cross-cutting conventions

- **Theme.** `src/theme.js` exports 3 palettes (`slate`, `sand`, `neutral`) plus `ThemeProvider`/`useTheme()`. `getTheme` falls back to `neutral` for unknown names. The default theme is `neutral` (grayscale). Components get colors from `const { colors } = useTheme()` and build styles per theme: `const styles = useMemo(() => createStyles(colors), [colors])` with `createStyles = (colors) => StyleSheet.create({...})` at the bottom of the file. There is no static `colors` export; never import a palette directly. `spacing`/`radius`/`fonts` are still static imports, as are the `cardShadow`/`panelShadow` elevation tokens (spread into a style object instead of re-declaring per-file shadow props). The palette also carries `statusBarStyle`, `keyboardAppearance` (use on every TextInput), `onAccent` (text on accent-filled surfaces), semantic `success`/`warning`/`danger`, `glowStart`/`glowEnd`/`glowWashTop` (the two hues of the Dashboard's top-of-page wash — `HeaderGlow` in DashboardScreen — plus the pre-blended solid App.js paints on the status-bar strip while the Dashboard tab is active; `glowWashTop` must stay in sync with the wash's 0.34 top stop opacity), and `backdrop`. Category emojis and colors are multi-colored (defined in `categories.js`); all other UI icons use monochrome text symbols styled with `colors.icon`.
- **Unified font system** (Liberation Sans family, bundled as TTFs in `assets/fonts/` with their OFL licenses, loaded with `useFonts` from `expo-font` in `App.js` before first render). One design drives BOTH text and numbers, so word labels align with the money/stat figures beside them. All non-bold text is slightly bolded by design: `fonts.regular`/`numRegular` resolve to `Arimo-Medium` (Arimo is the Google-Fonts continuation of Liberation Sans — metric-identical — supplying the 500 weight the Liberation family lacks), while `fonts.medium`/`bold`/`numMedium`/`numBold` all resolve to `LiberationSans-Bold` (num* styles additionally set `fontVariant: ['tabular-nums']`). Styles must NOT set `fontWeight` alongside `fontFamily` (Android mis-resolves when both are present). All monetary amounts, stats, chart labels, budget inputs, and percentages use the `num*` variants. (Inter and Lora were retired; nothing imports `@expo-google-fonts/*` any more.)
- **All UI strings go through i18n** (`src/i18n.js`: language codes `en` / `zh` (Traditional Chinese, label 繁體中文) / `es` — note the key is `zh`, not `zh-Hant`). Components: `const t = useT()`; non-component code: `translate(language, key, vars)`. Missing keys fall back to English, then the key. Category display names are `t('cat.' + id)` — `CATEGORIES[].label` is only the English fallback. Date labels in `src/format.js` take a `language` arg and read name tables from `i18n.js` (`getDateNames`); language-dependent derived data must re-compute when `settings.language` changes (it's a dep of the `deriveViewData` memo).

## Architecture

All state lives in `App.js`; everything under `src/` is presentational or a pure helper. Navigation is hand-rolled (no react-navigation): a `tab` state (`dashboard | list | split | insight`) drives four kept-mounted tab screens (absolutely positioned, hidden via animated opacity; a horizontal slide transition plays on each switch — `changeTab` in `App.js` drives it with `slideAnim`/`slideDirRef`/`prevTab`; the content container uses `overflow: 'hidden'` to clip off-screen screens during transitions; dismiss the keyboard on tab change). Keep `TAB_INDEX`/`TAB_NAMES` (App.js) and the `TABS` array (TabBar) in the same order. The add "+" is the center button of the tab bar (TabBar takes an `onAdd` prop); `openAdd()` opens the add popup (`AddExpenseModal`, kept mounted via `display: 'none'` so a half-typed entry survives dismissal). The add popup has a **Personal/Shared** header toggle (`EntryModeToggle`, driven by `addEntryMode` in App.js): Personal renders `AddEntryScreen` (add an expense), Shared renders `SharedSplitForm` (add a split bill). An `addNonce` state keys the Shared form so it remounts fresh on each open. An `overlay` state (`budget | account | createGroup | null`) drives the budget-editor, account, and create-group Modal sheets; `activeGroupId` drives the group-detail sheet. A group's "Add a bill" reuses the Shared add popup locked to that group (`sharedLockedGroupId`, set by `openSharedAddForGroup`; `closeAdd` reopens the group sheet afterward). Every tab shows a centered ‹ month › selector (the shared `MonthSelector` component) under its page title, and each tab's month is INDEPENDENT — changing the month on one page never affects another. Dashboard's lives in App.js (`dashMonthKey`/`shiftDashMonth`, because `heroView` derives there) and scopes the hero card + category summary card; Expenses, Insight and Split own theirs as local screen state (kept-mounted tabs preserve it). Split's is deliberately display-only — balances are outstanding debts and stay all-time. The category card's "more detail" pill navigates to the Insight tab (`changeTab('insight')`), which hosts the full per-category tile grid on its Categories card (there is no separate breakdown page any more). Account is no longer a tab — it opens from a floating account button (`accountFab` in `App.js`) pinned top-left, the only floating FAB (the add "+" lives in the tab bar). It's gated by a single `chromeVisible` flag that hides it whenever any popup or sheet is open (`overlay`, `addOpen`, `editingExpense`, `activeGroupId` — extend it if you add another popup/sheet). There are NO early returns inside the main component: every hook runs unconditionally, then a single `let content` is assigned by an if/else chain — `AuthScreen` when Supabase is configured and there's no session, `null` while data loads, `OnboardingScreen` until onboarding is done, else the main UI — and one `return` at the end wraps `content` in `ThemeProvider` + `I18nProvider` (fed from `settings.theme` / `settings.language`). Keep new conditional logic in the `content` assignment, and put any new top-level chrome (FABs, sheets) inside the main-UI branch so it never renders over Auth/Onboarding. (The only early `return` is the font-loading guard in the outer `App()` wrapper.)

- `App.js` — root component. Owns `expenses` (`{id, amount, currency, note, category, createdAt}`, amount in the ENTRY currency), `income` (`{id, amount, currency, source, note, createdAt}` — a parallel entity to expenses, amount in the ENTRY currency), `settings` (`{displayCurrency, monthlyBudget, categoryBudgets, customCategories, theme, language, onboardingDone}`), `session` (Supabase auth), nav state, and `rewardNonce` (increments per added expense to trigger RewardCheck). Data flow is cache-first: on sign-in (or launch) the per-user AsyncStorage cache renders immediately, then `syncWithServer()` reconciles with Supabase in the background and on each return to foreground. A `dataUser` state records which user the in-memory data belongs to; the save-to-cache effects are gated on `dataUser === userId` so account switches can't write one user's data under another's cache key. Every mutation updates state AND enqueues a sync op (`enqueueExpenseUpsert`/`enqueueExpenseDelete`/`enqueueExpensesReplace`, `enqueueIncomeUpsert`/`enqueueIncomeDelete`/`enqueueIncomeReplace`, `enqueueSettingsPush`) — if you add a mutation, do both or devices drift. Income syncs through a SEPARATE per-user queue (keyed `${userId}::income`) so a problem on one lane (e.g. the `income` table not yet created) can never wedge the other; the income pull is tolerant (errors leave income null and the cache stands). `deriveViewData(expenses, displayCurrency, language)` computes everything the UI shows in one pass, all converted to the display currency: day-grouped sections (items gain `displayAmount`), current-month stats (`monthTotal`, `lastMonthTotal`, `todayTotal`, `avgPerDay`), per-month aggregates (`months[].byCategory`, plus `months[].dailyTotals` — each month's own per-day series, feeding the hero chart for whichever month is selected) for the Categories tab, and `dailyTotals` (per-day spending for the current month, used by `SpendingChart`). Category keys are normalized through `getCategory()` so stale ids group under "Other" everywhere. When the display currency changes, `updateSettings` re-denominates the stored overall budget AND every category budget via `convert()`. Income entries (like expenses) are stored in their entry currency and converted at display time, so they need no re-denomination. When Supabase is configured and the user hasn't completed onboarding (`!settings.onboardingDone`), App.js shows `OnboardingScreen` before the main UI.
- A `dayStamp` state value (today's date key, refreshed every minute and on app-resume via AppState) is a dependency of that memo so stats roll over at midnight even though `expenses` hasn't changed. Date-sensitive derived data must depend on it (`currentMonthKey` for the Categories tab is derived from it).
- **Settings sync.** The Supabase `settings` row carries `displayCurrency`, `monthlyBudget`, `categoryBudgets`, `categoryOrder`, `customCategories`, `customPaymentMethods`, `firstName`, `lastName` (see `pickSyncedSettings`/`toSettingsRow`/`fromSettingsRow` in `sync.js`); only per-device preferences (`theme`, `language`, `onboardingDone`) stay device-local. The extra columns are NULLABLE — NULL means "no device pushed this yet" and `fromSettingsRow` omits it, so the merge over local (`setSettings(prev => ({...prev, ...result.settings}))` — never replaced) can't clobber a local value with an empty default. `updateSettings` only enqueues a push (of the whole synced subset) when a synced field actually changed; device-local-only patches must not bump `settingsVersionRef`. The settings push falls back to the legacy two-column payload on an unknown-column error (database not yet migrated with `supabase/migrate-settings-sync.sql`), so it can never wedge the expense lane; App.js seeds the new columns from local values on the first pull that finds them unset. Because `categoryBudgets` sync with the currency, a currency change on another device now arrives already re-denominated.
- `src/currency.js` — `CURRENCIES` (code/symbol/name/decimals/flag), static `RATES_TO_USD`, and `convert(amount, from, to)`. **Conversion happens ONLY here** (single helper by design — swapping in a live-rate API is a one-function change). Amounts are stored in their entry currency forever; conversion is a display-time concern.
- `src/screens/` — one file per screen: `DashboardScreen` (a centered greeting row ("Hi, {name}" — symmetric padding clears the account FAB so it centers on the screen) with this tab's `MonthSelector` under it (drives `dashMonthKey`/`shiftDashMonth` in App.js — past months plot in full via `SpendingChart`'s `endDay` prop, forward nav caps at the current month, empty months show $0 honestly); a Copilot-style top-of-page wash (`HeaderGlow` — an SVG `LinearGradient`, `glowStart`→`glowEnd`→transparent, 240px tall, FIXED behind the transparent ScrollView so content scrolls over it; it replaced the old `CardGlow` corner bloom, and App.js paints the status-bar strip in `glowWashTop` while this tab is active so the wash starts at the physical top — because of that tinted strip, every tab screen must paint its own background); a Monthly Spending hero card — selected-month total, a month-over-month delta badge, embedded `SpendingChart` (the display-currency pill moved to the Insight page's Budget card); a category summary card (`CategorySummaryCard`) — a "Categorical spending" title (`cats.sectionTitle`, mirroring the Split balances title below) + a donut + Most/Least-spending stats for the selected month (no month nav of its own) and a "more detail" pill that jumps to the Insight tab; a split-balances widget (below the category card); and the empty state — the budget section moved off the Dashboard to `InsightScreen`), `AddEntryScreen` (the Personal side of the add popup — add/edit an EXPENSE only, rendered inside `AddExpenseModal`. A paginated category selector (a horizontal `ScrollView` with `pagingEnabled` showing 8 categories per page in a 4×2 grid with dot indicators), a `CalendarField` date picker, a numeric amount input (sanitized via `cleanAmountInput`), a tappable currency pill (`CurrencyPill`, opens the shared `CurrencyPicker`) on the left of the amount, and a note with counter. The card's border/background tint animates to the selected category color. When adding, the header shows the Personal/Shared toggle (`EntryModeToggle`); in edit mode it shows `edit.title` plus a red Delete button (wired to `deleteExpense` via the shared `confirmDestructive`). Card chrome comes from the shared `popupChromeStyles`. On add it resets and the popup closes — the RewardCheck overlay is the only confirmation. (The old income mode and the ÷ split tool were removed.)), `SharedSplitForm` (the Shared side of the add popup — create a split bill: a `CalendarField` date, a currency pill (`CurrencyPill` → `CurrencyPicker`) on the left of the amount, and a compact **Group / Paid by / Split method** selector-chip row — three label-over-value chips side by side, each opening a centered `OptionPicker` popup (a "create a group" CTA shows when there are no groups). Participants render as tappable avatar-initial chips in equal mode with a single "≈ {amount} each" caption (`split.eachShare`; no per-row previews); custom/percentage/tax keep one slim avatar row per person carrying that person's inline input, and the tax%/tip% fields are inline-label pills. Split methods: equal / custom amounts / percentage / **tax** (itemized: per-person subtotals plus a tax% and optional tip% distributed proportionally to subtotals). On save it computes per-person shares via `splits.js` and calls `addSplitExpense`. Reused both from the "+" popup (group picker shown) and from a group's "Add a bill" (locked to that group, picker hidden)), `ExpenseListScreen` (its own `MonthSelector` under the title — the SOLE month control (the calendar card's header nav was removed); stepping it moves the calendar month and selects today in the current month or the 1st otherwise; inline calendar for day selection, category filter chips, expense rows for the selected day, custom delete-confirmation Modal), `InsightScreen` (tab, 5th slot: the budget view compressed into two cards. A "Budget" card with a compact horizontal bar gauge (the `BudgetBar` component in this file: remaining/over figure + % used, a slim zone-colored green/yellow/red progress bar, and the spent-of-budget line; the old `BudgetGauge` donut was retired) whose header carries the display-currency `CurrencyPill` and "Edit budgets" (`onEditBudgets` → Budget editor sheet). Below it a merged "Categories" card: a two-column grid of category widget tiles (the style of the retired `CategoryBreakdownScreen` — tinted cell with color-keyed left edge, circular icon chip, month amount (always shown; "$0" is the no-activity placeholder) and month-over-month delta (up = red, down = green, "new" when last month was empty); every tile carries a progress bar along its bottom — a dimmed category-tinted track that fills against the budget, an empty strip when none is set; budgeted categories add "/ budget" to the amount), plus an "External" subsection (divider with total + external tiles). Tiles order by `settings.categoryOrder` (the breakdown page's long-press drag-to-reorder, restored as `DraggableTileGrid` in this file — one order array spans both sections; unordered ids append spend-sorted; the grid denies pan termination so the tab-swipe responder can't steal a drag). A tile renders for any category with activity this/last month, a budget, or `custom: true`. The card header's "+ Add Category" pill (and tapping a custom tile) opens `AddCategoryModal`, so custom-category management lives here. Receives `monthlyBudget`/`categoryBudgets`/`regularCategories`/`externalCategories`/`months`/`currentMonthKey`/`categoryOrder` from App.js; owns its month selection locally (a `MonthSelector` under the title, seeded from `currentMonthKey`) and derives that month's `totalsByCategory` from `months[].byCategory`, so the budget gauge and tiles follow this page's own ‹ month › selection. Replaced the old Income/Balance screen — income can no longer be added or viewed in the UI; the income data/sync layer is retained but inert (no income UI remains — the income-edit popup and `AddEntryScreen`'s income mode were removed)), `AccountScreen` (near-full ~92% bottom Modal sheet opened by the floating account button, not a tab: name fields, account info/sign-out, a "Delete account" danger action (wires `deleteAccount` in App.js — wipes server + device data and signs out; the Supabase auth record itself needs a server function to remove), language + theme switcher; presented via the shared `Sheet` component with `showHandle`), `BudgetScreen` (Modal sheet: overall monthly budget, regular category budgets each with a proportion slider that snaps to 5% of the overall budget, external category budgets in a separate section; the display currency is chosen on the Insight Budget card header, not here), `AuthScreen` (email/password sign-in/sign-up, full-screen when signed out; errors render inline because `Alert` is a no-op on web), `OnboardingScreen` (first-run setup: monthly budget + language; shown only when Supabase is configured and `onboardingDone` is false).
- `src/components/` — `TabBar` (exports `TAB_BAR_HEIGHT`; position absolute at the bottom with frosted glass blur via `expo-blur`; four tab icons (Dashboard · Expenses · Split · Insight) plus a center add "+" button (takes an `onAdd` prop; the "+" is NOT a tab, so it stays out of TAB_INDEX/TAB_NAMES); screens must add `paddingBottom: TAB_BAR_HEIGHT` to their ScrollView contentContainerStyle so content isn't hidden behind it), `AddExpenseModal` (popup presenter: backdrop fade + card scale/fade, keeps children mounted while closed), `RewardCheck` (full-screen success overlay with a centered animated gradient checkmark; total on-screen time is SHOW_MS + FADE_MS — tuned to ~0.88s after two successive 30% reductions, don't lengthen without asking), `SpendingChart` (SVG line chart: smoothed cubic Bezier path of daily totals with gradient fill, peak marker, dashed horizontal grid lines with y-axis labels, x-axis day labels; accepts `quickStats` prop for inline stats header), `CategorySummaryCard` (the Dashboard donut card moved off the old Categories tab — `CategoryDonut`/`ExtremeStat`/`buildArcs`/`formatPct` live here, plus a month nav and the "more detail" pill that jumps to the Insight tab via `onMoreDetail`), `AddCategoryModal` (the add/edit-custom-category Modal — name, REQUIRED monthly budget (min 5% of the overall budget, any positive amount when none is set; saved into `settings.categoryBudgets` by App.js' add/updateCustomCategory, which strip the `budget` field off the returned object), paginated icon grid, preset colors + hue-slider custom color, external switch; opened from the Insight Categories card), `CalendarField` (the shared expanding date picker used by both add forms; exports `dateForOffset`/`offsetForDay`), `EntryModeToggle` (the Personal/Shared segmented toggle), `MonthSelector` (the centered ‹ month › selector rendered under every tab's page title; `monthKey`/`currentMonthKey`/`onShift` props, forward nav capped at the current month — each tab feeds it its OWN independent month state), `OptionPicker` (the shared compact centered picker popup used by the Shared add form for Group / Paid by / Split method, mirroring `CurrencyPicker`), `popupFormChrome` (`popupChromeStyles` — the shared add-popup card/header chrome), `Sheet` (the shared bottom-sheet Modal presenter — backdrop + slide-up shell used by `AccountScreen`, `BudgetScreen` and `GroupDetailScreen`; `showHandle`/`avoidKeyboard`/`sheetStyle` props), `EmptyState` (the shared no-data view — icon, hint, "add first" button, "load demo" link — used by Dashboard, Expense List and Insight), `ErrorBoundary` (class-component crash fallback wrapping the whole app in App.js with `resetKeys={[userId]}` so a sign-out/account switch clears the fallback), plus `ExpenseRow`.
- `src/storage.js` — AsyncStorage load/save for expenses, income and settings, the local cache layer; all swallow errors by design (persistence is best-effort, in-memory state is the source of truth). Keys are scoped per user id; `LOCAL_USER` (`'local'`) is the unconfigured-mode sentinel and keeps the original un-suffixed keys so pre-auth installs retain their data. `loadExpenses`/`loadIncome` normalize legacy entries (missing `currency` → USD; income also defaults `source`/`note`); `loadSettings` merges `DEFAULT_SETTINGS` and guarantees a fresh `categoryBudgets` object so old caches upgrade in place.
- `src/supabase.js` — the Supabase client (null when env vars are unset) + `isSupabaseConfigured`. Auth sessions persist in AsyncStorage, NOT the expo-sqlite localStorage shim from the Expo guide — AsyncStorage also works on react-native-web. Pauses token auto-refresh while backgrounded on native.
- `src/sync.js` — offline-first sync engine. Mutations enqueue durable ops (per-lane pending-ops queue in AsyncStorage, coalesced); `flush()`/`flushIncome()` push them in order, stopping at the first failure; `syncWithServer()` = flush both lanes, then pull all rows, with `applyPendingOps()`/`applyPendingIncomeOps()` re-applying anything still queued on top so offline edits survive a pull. **Expenses and income use SEPARATE queues** (keys `userId` and `${userId}::income`) sharing the same generic load/persist/flush helpers (`flushQueue`), so one lane can't wedge the other; the income pull is tolerant (a missing `income` table or any error returns `income: null` and the cache stands). Cross-device conflicts: last write to the server wins per row. The client never sends `user_id` — the column default (`auth.uid()`) and RLS policies (see `supabase/schema.sql`) scope every query to the signed-in user.
- `src/categories.js` — 8 built-in categories (`CATEGORIES`) plus user-created custom categories stored in `settings.customCategories` (synced via the settings row). `external` flag separates bills-like categories from budget tracking. Every function takes `customCategories` as a parameter (not module-level state): `getAllCategories(customCategories)` / `getRegularAll(customCategories)` / `getExternalAll(customCategories)` return merged lists; `getCategory(id, customCategories)` searches both and falls back to "Other". `getCategoryLabel(category, t)` resolves display names (i18n for built-in, raw label for custom). `generateCategoryId()` creates a unique id for new custom categories. `EMOJI_OPTIONS` (24 icons, displayed in paginated pages of 14) and `COLOR_OPTIONS` (18 preset colors) are curated palettes for the add-category picker. Screens receive merged category lists as props from App.js; built-in categories cannot be deleted.
- `src/incomeSources.js` — 6 fixed income sources (`INCOME_SOURCES`: salary/freelance/side_income/reimbursement/gift/other), each with a muted color dot. No user-created sources. `getIncomeSource(id)` falls back to "Other"; `getIncomeSourceLabel(source, t)` resolves the i18n name (`t('source.' + id)`). Mirrors `categories.js`'s shape.
- `src/format.js` — money and date formatting. `formatMoney`/`formatMoneyShort` take a currency code (symbol + decimals per currency; JPY/TWD render with 0 decimals). `dateKey()` (local-time `YYYY-MM-DD`) is the canonical day identity; `monthKeyLabel()` formats a `YYYY-MM` key as a human-readable month/year string. `buildCalendarWeeks(year, month)` returns a grid of 7-cell rows for calendar UIs (shared by `CalendarField` and `ExpenseListScreen`). `cleanAmountInput(text)` strips everything but digits and decimal separators for the add forms' amount/share inputs. Date-label functions take a `language` arg.
- `src/budget.js` — pure budget-allocation math for the budget editor: ratio↔amount conversion against the overall budget, 5% slider snapping (`snapRatioToStep`), and clamping so the sum of category budgets never exceeds the overall (`clampCategoryBudgetAmount`/`maxBudgetForCategory`; `fitAllocatedBudgetsToOverall` proportionally shrinks existing allocations when the overall budget drops).
- `src/confirm.js` — `confirmDestructive({ title, body, confirmLabel, cancelLabel })`: the one web/native confirm helper (native `Alert.alert`, `window.confirm` on web), returning a `Promise<boolean>`. Used by every destructive action (delete expense/bill/group, settle, sign out, delete account, load demo) instead of re-implementing the Alert/web fallback inline.
- `src/icons.js` — registry wrapper around `@hugeicons/react-native`; maps string keys (e.g. `'hamburger-01'`) to imported icon components. `HIcon` is the single render entry point. Category items and all UI chrome use this; never import icon components directly. `HIcon` forwards the `style` prop directly to `HugeiconsIcon` (both the matched and fallback branches pass it through).
- `src/derive.js` — `deriveViewData(expenses, displayCurrency, language, customCategories, now, extraSpending)` — single-pass derivation of all display data (day sections, month stats, category totals, daily chart). Called as a memo in App.js. `extraSpending` (from `yourShareAsExpenses`) folds split-bill shares into aggregates without adding them to the Expenses list sections. `now` is injectable for deterministic tests.
- `src/demoData.js` — sample expenses (`buildDemoExpenses`) and income (`buildDemoIncome`) for the empty-state "Load demo data" button; both span three calendar months and mix currencies so the Categories trends, the Income/Balance chart, and conversion always have something to show.

## Planned refactor

`.claude/screen-contracts.md` describes an alternative layout (3-tab bar, AddExpense as a full tab screen, Settings and Compare as separate Modals). It has NOT been implemented — the current code matches the Architecture section above. Read that file if asked to restructure screens.

## Screen budget

The original product spec capped the app at 10 distinct views; the Split Bills feature deliberately expanded that. Current views: Dashboard (also hosts the category summary card), Add (popup — Personal expense via `AddEntryScreen`, or Shared bill via `SharedSplitForm`), Expense List, Split Bills (tab), Insight (tab — budget overview + category tiles + custom-category management), Group detail (sheet), Create group (sheet), Account (sheet, opened by the floating account button), Budget editor (sheet), Auth, Onboarding (first-run only). The add popup uses `AddEntryScreen`/`SharedSplitForm` inside the `AddExpenseModal` presenter. The tab bar holds four tabs (Dashboard, Expenses, Split, Insight) with the add "+" as its center button; the Categories tab was retired (its summary card moved to the Dashboard) and the separate Category-breakdown page was later folded into the Insight page's Categories card. Income can no longer be added or edited from the UI — the income-edit popup and `AddEntryScreen`'s income mode were removed; the income data/sync layer remains but is inert. The separate add-bill sheet (`AddSplitScreen`) was retired — bills are now added through the Shared add popup.

## Split Bills

A Splitwise-style ledger synced to Supabase via two separate lanes (`groups` and `split_expenses` tables, queue keys `${userId}::groups` and `${userId}::splits`). Pulls are tolerant like income — if the tables don't exist yet, the local cache stands. `src/splits.js` is the pure domain layer: the `YOU` owner sentinel, `PAYMENT_METHODS`/`getPaymentMethod`/`getPaymentMethodLabel`/`getPaymentMethodColor`/`getAllPaymentMethods`, `computeShares` (equal/custom/percentage, currency-decimal aware, shares always reconciled to sum to the bill), `customSharesValid`/`percentageSharesValid`, `computeTaxShares` + `taxInputValid` (the itemized tax split — per-person subtotals plus a tax%/tip% distributed proportionally), `groupBalances`/`groupNet`/`overallBalance` (a personal ledger — only debts involving YOU; positive = a member owes you), `yourShareAsExpenses`, `removeMemberFromBill` + `billUndistributed` (member-removal share handling — see below), plus `GROUP_ICONS`/`DEFAULT_GROUP_ICON` (emoji avatars) and `PAYMENT_ICON_OPTIONS`. **Payment methods are category-like**: each built-in AND custom method carries a `color` + a hugeicon `icon` (rendered as an icon+color chip and used to theme a group's surface). Built-ins are fixed (cash/card/bank/mobile/other); users add custom methods via `PaymentMethodModal` (name + icon + color/hue picker, mirroring the custom-category editor) — stored in `settings.customPaymentMethods` (synced via the settings row). `getPaymentMethod`/`getPaymentMethodColor` fall back to "cash" for unknown ids, and `getPaymentMethodLabel` likewise falls back to the cash label (never echoes a raw id). Groups hold members (typed names, no accounts), a `currency`, a `paymentMethod`, and an `icon` (emoji avatar, device-local, picked via the `IconPickerSheet` page) — there is NO group "type/category". `splitExpenses` hold shared bills (`{id, groupId, description, amount, currency, category, paidBy, mode, shares, createdAt}`, plus an optional device-local `meta` holding the raw percentage/tax inputs so an edit can reconstruct them losslessly) and settlement records (`{settlement: true, from, to, ...}`). **`group.icon` and bill `meta` are device-local** (not in the Supabase `groups`/`split_expenses` column mappings); App.js's sync pull-merge preserves them by id across reconciles, same posture as theme/language. Local cache: `loadGroups`/`saveGroups`/`loadSplitExpenses`/`saveSplitExpenses` in `storage.js`; sync: `enqueueGroupUpsert`/`enqueueGroupDelete`/`enqueueSplitUpsert`/`enqueueSplitDelete` in `sync.js`. App.js owns `createGroup`/`updateGroup` (edit currency/payment method/icon/members in place)/`removeGroupMember`/`deleteGroup`/`addSplitExpense`/`updateSplitExpense`/`deleteSplitExpense`/`settleUp`/`addCustomPaymentMethod`/`removeCustomPaymentMethod` (reassigns affected groups to cash). **Member removal is bill-aware** (the × in `GroupDetailScreen`): unreferenced members are removed outright; a member who PAID a bill can't be removed (edit/delete those bills first — `alertInfo` in `confirm.js` explains); a share-holder gets a choice popup (`OptionPicker` with a `subtitle`) — **redistribute** (their share is re-split among the remaining participants via `removeMemberFromBill`: equal bills stay equal, weighted bills scale proportionally) or **reassign manually** ('unassign': the share key is dropped, the bill becomes a 'custom' split with an undistributed residual — shown on the bill row via `billUndistributed`, and with exactly one affected bill the editor opens on it immediately). Their settlements are deleted with them. Committing a member-name blur never removes anyone (an emptied name reverts). Screens: `SplitBillsScreen` (tab, centered title with a `MonthSelector` under it that is DELIBERATELY display-only — balances are outstanding debts, so the summary and group cards stay all-time regardless of the selected month (product decision; don't wire it into balance math without asking); groups render as a two-column grid of square rounded widget tiles — avatar, name, members · method, net balance — each accented by its payment-method color along the top edge), `GroupDetailScreen` (members FIRST — the combined members+balances card leads the sheet — then a payment-method-THEMED settings card — animated border/wash tint to the method's color, mirroring the add-expense category tint — with icon+color method chips, then the bill list; the currency pill sits in the title row, the avatar is a tappable emoji; cards use a `cardShadowWrap` so `overflow:'hidden'` doesn't suppress the iOS shadow), `CreateGroupScreen` (sheets); bills are added through the Shared add popup (`SharedSplitForm`) and EDITED by tapping a bill row (`SharedSplitForm` in `editBill` mode — re-decide paid-by / split method / shares; reuses the group-sheet round-trip via `editingSplit` in App.js). **"Your share counts as spending":** `yourShareAsExpenses(splitExpenses)` synthesizes expense-like items for your share of each bill, passed to `deriveViewData` as its 6th `extraSpending` arg — these fold into every spending aggregate (month total, category totals, daily chart, per-month breakdown, budgets) but are deliberately kept OUT of the Expenses list `sections` (so the list stays direct-expenses-only). The Dashboard shows a split-balances widget; balances display in each group's own currency, the overall summary converts to the display currency.

## Claude Code setup

The Expo Claude Code plugin is enabled (`.claude/settings.json`). `.claude/agents/` defines a team of persona agents (angelina, friday, monday, tuesday, saturday, sunday) for structured task delegation — see `.claude/agents/_team.md` for roles. `.claude/workflows/` holds multi-agent Workflow scripts (e.g. `expense-tracker-full-review.js`); agents keep per-persona notes in `.claude/agent-memory/`.

**Currency picking is unified:** anywhere a currency is decided (the Insight Budget card header for the display currency, Create/Edit group, and the Personal/Shared add forms — on the left of the amount input) renders a `CurrencyPill` (a small code pill) that opens the shared `CurrencyPicker` (`src/components/CurrencyPicker.js` — a small centered "Choose currency" popup widget, NOT a full-height sheet/page: a compact `Modal` (fade) over a centered card with a short scrollable list of `CURRENCIES`, no search field. Each row badges the country's `flag` emoji in a circle. Tapping a row selects + closes). The legacy `CurrencyDropdown` is retired. **Card-header pills share one look** (the SplitBills "New group" pill is the reference): soft accent-tinted fill (`` `${colors.accent}15` ``), borderRadius 14, bold accent text, pressed state = opacity 0.6 — no border or shadow. `CurrencyPill`, the Insight header pills, and the Dashboard "more detail" pill all follow it. `CURRENCIES` now has 13 entries (added KRW/AUD/CAD/CHF/HKD/SGD/INR with static `RATES_TO_USD`), each carrying a `flag` emoji (renders natively on iOS/Android/macOS; Windows browsers fall back to the 2-letter region code).
